func main() {
    let x = 2;
    let y = 4;

    if (x > y) {
        let z = 10;
        let sum = x + y + z;
        return sum;
    } else if (x < y) {
        let diff = x - y;
        return diff;
    } else {
        return 0;
    }

}